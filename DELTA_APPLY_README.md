# Geometry-MMALS G1 — GitHub delta v1.1.0 to v1.1.2

Baseline:

`Geometry_MMALS_G1_GitHub_Package_v1_1_0_RESULTS_v1_1_1_PLAN.zip`

This archive contains only files that are new or byte-changed relative to that
last v1.1.0 GitHub package. It contains no unchanged historical files.

## Apply

Extract this ZIP into the parent directory containing:

```text
geometry-mmalls-g1/
```

Allow replacement of existing files. No file deletion is required.

## Large execution evidence

The five `dense_trace.pkl` files and the redundant nested
`Geometry_MMALS_G1_v1_1_1_Corrected_Results.zip` are intentionally omitted
from this Git-oriented patch. They account for most of the 813 MB full package
and are not appropriate for a normal Git commit.

The aggregate and per-seed CSV evidence, corrected manifests, reviewer report,
v1.1.2 specification, notebook, configurations, source changes, tests, and
verification files are included.

See:

- `DELTA_MANIFEST.json`
- `DELTA_FILE_INDEX.csv`
- `EXCLUDED_LARGE_EVIDENCE_SHA256SUMS.txt`
