# Geometry-MMALS G1 v1.1.0 results and v1.1.1 plan release

## Reused from the cross-branch research instructions

The release adopts the operator-level view

```text
R_theta : P(Z x M) -> P(H)
```

and keeps goal-conditioned routing

```text
R_theta : P(Z x M) x G -> P(H)
```

inside the TPUT branch. It also integrates the following requirements:

- local continuity distributions rather than mean-only sensitivity;
- explicit perturbation and drift tests;
- route-function coherence and host-specialization evidence;
- metric comparison rather than automatic preference for non-Euclidean geometry;
- v1.1.2 SPD covariance evaluation;
- v1.1.3 route-function memory transport;
- v1.1.4 causal specialization and cross-seed qualification;
- an independent metric, protocol, and claim verification stack.

## v1.1.0 executed result

- context preservation and matched compute: PASS;
- prototype nominal route mediation: candidate positive on trained and held-out factors;
- prototype functional mediation: candidate positive on trained factors only;
- linear router: at least as competitive, strongest exploratory causal/ablation signals;
- prototype causal specificity, identity preservation, and host ecology: FAIL;
- operational non-degradation: PASS as equivalence only;
- memory transport and operational superiority: NOT TESTED.

## v1.1.1 primary correction

Freeze one common context, host bank, classifier, and functional host-cost matrix. Compare MLP, linear, prototype, and hybrid directional-prototype routing. Promote held-out functional mediation, local continuity tails, route swaps, and causal identity-preserving perturbations to primary gates.
