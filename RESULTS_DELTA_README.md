# Geometry-MMALS G1 v1.1.2 executed-results delta

This archive updates the previously committed v1.1.2 C0 repository with the
completed five-seed results release.

Extract it into the parent directory containing `geometry-mmalls-g1/` and allow
overwrite.

The delta includes:

- corrected `results/v1_1_2/` evidence and manifests;
- corrected canonical and results-preserving notebooks;
- the reviewer article PDF, figures, data, and LaTeX source;
- the clean 47-page execution archive;
- independent verification outputs;
- updated verification gate aliases and NOT_TESTED handling;
- updated README, status, changelog, manifest, validation, and release notes;
- regression test for non-tested experiment gates.

The redundant release ZIP
`releases/v1.1.2/Geometry_MMALS_G1_v1_1_2_Corrected_Results.zip`
is omitted because it duplicates `results/v1_1_2/`. Its checksum is recorded in
`EXCLUDED_REDUNDANT_ASSET_SHA256SUMS.txt`.
