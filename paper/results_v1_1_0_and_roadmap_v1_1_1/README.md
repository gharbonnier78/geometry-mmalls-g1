# Geometry-MMALS G1 v1.1.0 results article

Compile with:

```bash
latexmk -pdf -interaction=nonstopmode main.tex
```

The package includes the article source, references, figures in PDF and PNG formats, and immutable selected CSV/JSON evidence from the five-seed v1.1.0 run.
