# Geometry-MMALS G1 v1.1.3 C0-r2 specification

Compile with:

```bash
pdflatex main.tex
pdflatex main.tex
```

This revision incorporates the pre-pilot reviewer corrections: remembered-source budget semantics, explicit temporal notation, acquisition ordering, exact functional-cost normalization, fixed Sinkhorn and temperature parameters, trace-matched isotropic baseline, metric-based reconstruction acceptance, mandatory gradient-scale calibration, and a frozen-regime stopping rule.
